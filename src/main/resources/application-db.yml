spring:
  datasource:
    url: jdbc:mysql://mysql:3306/novamindsDB?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: novaminds1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true

  docker:
    compose:
      enabled: true
      file: compose.yaml
      lifecycle-management: start_and_stop