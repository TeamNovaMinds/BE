spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  profiles:
    include:
      - prod
      - oauth
      - jwt
      - s3
      - email
      - redis
server:
  address: 0.0.0.0
  port: 8080
